{ ... }:

# Placeholder. Replace this with the generated file from:
#   sudo nixos-generate-config --show-hardware-config > /etc/nixos/hardware-configuration.nix
# or copy the generated /etc/nixos/hardware-configuration.nix into this path.
{
}
